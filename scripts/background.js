/**
 *
 * Runs in background on events like onInstalled, onClicked, onMessage etc.
 * It's a service worker. So it'll be alive to the lifespane of the listener itself
 * It won't be active throughout the browser session
 *
 */
